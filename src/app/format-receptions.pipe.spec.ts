import { FormatReceptionsPipe } from './format-receptions.pipe';

describe('FormatReceptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatReceptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
